{% extends "FuelTechSupportBundle::layout.html.twig" %}

{% form_theme form 'FuelTechSupportBundle:Form:fields.html.twig' %}

{%block stylesheets%}
    {{parent()}}
    <link rel="stylesheet" href="{{asset('bundles/fueltechsupport/css/forms.css')}}">
{%endblock%}

{%block title%}
    {{parent()}}
    Klant
{%endblock%}
{%block content%}
    {#<div>
        
        <p>Naam: {{client.name}}</p>
        <p>Adres: {{client.address}}</p>
        <p>Postcode: {{client.postcode}}</p>
        <p>Stad: {{client.city}}</p>
        <p>Telefoon: {{client.telephone}}</p>
        <p>Fax: {{client.fax}}</p>
        <p>Email: {{client.email}}</p>
        <p>Opmerkingen:<br>{{client.remark}}</p>
    </div>
    <div>
        <h2>Contactpersonen</h2>
        {%for contact in client.contacts%}
        <div>
            {{contact.name}} {{contact.telephone}} {{contact.mobile}} {{contact.email}}
        </div>
        {%endfor%}
    </div>#}
    <h2>Klantgegevens</h2>
    {{form(form, {'attr':{'novalidate':'novalidate'} })}}
    
    <h2>Contactpersonen</h2>
    <table class="table">
        <thead>
            <tr>
                <td>Naam</td>
                <td>Telefoon</td>
                <td>Mobiel</td>
                <td>Email</td>
            </tr>
        </thead>
        <tfoot></tfoot>
        <tbody>
            {%for contact in contacts%}
            <tr>
                <td><a href="{{path('ftsupport_contact_detail', {'id':contact.id})}}">{{contact.name}}</a></td>
                <td>{{contact.telephone}}</td>
                <td>{{contact.mobile}}</td>
                <td>{{contact.email}}</td>
            </tr>
            {%endfor%}
        </tbody>
    </table>
{%endblock%}
